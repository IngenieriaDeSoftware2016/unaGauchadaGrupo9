<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Puntaje</th>
      <th>Editar</th>
      <th>Borrar</th>
    </tr>
  </thead>
  <tbody>
    <% Logro.all.each do |l| %>
    <tr>
    <!--<th scope="row"><%= l.id %></th>-->
    <td><%= l.nombre %></td>
    <td><%= l.puntaje %></td>
    <td><%= link_to '<i class="glyphicon glyphicon-pencil"></i>'.html_safe, edit_logro_path(l), class: 'btn btn-warning'%></td>
    <td><%= link_to '<i class="glyphicon glyphicon-remove"></i>'.html_safe, l, method: :delete, :class => 'btn btn-danger'%></td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Crear', new_logro_path#, class: 'btn btn-info'%>

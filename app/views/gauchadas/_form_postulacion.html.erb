<%@postulante=Postulante.new%>
<%= form_for(@postulante) do |p| %>

<div class="field">
  <%= p.label :nombre %><br />
  <%= p.text_field :usuario_id,required: true, autofocus: true %>
</div>


  <div class="actions">
    <%= p.submit "Crear" %>
  </div>
<% end %>

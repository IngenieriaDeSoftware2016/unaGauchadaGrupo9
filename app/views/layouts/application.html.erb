<!DOCTYPE html>
<html>
<head>
  <title>UnaGauchadaGrupo9</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <p class="navbar-text pull-right">
<% if usuario_signed_in? %>
  Logged in as <strong><%= current_usuario.email %></strong>.
  <%= link_to 'Edit profile', edit_usuario_registration, :class => 'navbar-link' %> |
  <%= link_to "Logout", destroy_usuario_session, method: :delete, :class => 'navbar-link'  %>
<% else %>
  <%= link_to "Sign up", new_usuario_registration, :class => 'navbar-link'  %> |
  <%= link_to "Login", new_usuario_session, :class => 'navbar-link'  %>
<% end %>

  <%= render 'layouts/header' %>

  <div id="main-container" class="container">
      <div class="row">
        <div class="col-xs-3">
          <h3>UnaGauchada</h3>
        </div>
        <div class="col-xs-9">
          <% if flash[:notice] %>
            <div class="alert alert-info"> <%= flash[:notice] %> </div>
          <% end %>
          <%= yield %>
        </div>
      </div>
  </div>
  <%= render 'layouts/footer' %>

</body>
</html>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <div class="dropdown">
        <ul class="nav navbar-nav">
          <li class="active"><a href="#">Home</a></li>
          <% if usuario_signed_in? %>
          <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">Sesion <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Mi Perfil', usuario_path%></li>
              <li><%= link_to 'Editar Perfil', edit_usuario_registration_path%></li>
              <li><%= link_to "Logout", destroy_usuario_session_path, method: :delete%></li>
            </ul>
          </li>
          <li><%= link_to 'Compras', new_compra_path%></li>
          <%if current_usuario!=nil%>
            <%if current_usuario.admin%>
              <li><%= link_to 'Menu Logros', logros_path, class: 'navbar-link'%></li>
            <% end %>
          <% end %>
          Logged in as <strong><%= current_usuario.email %></strong>.
          <% else %>
            <li><%= link_to "Registrarse", new_usuario_registration_path, :class => 'navbar-link'  %></li>
            <li><%= link_to "Login", new_usuario_session_path, :class => 'navbar-link'  %></li>
        <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
